# Install

This is a thin alias. The full install matrix — three tracks (local dev,
The full install matrix — three tracks (local dev, Docker Compose,
production) × three operating systems (Windows, macOS, Linux) — lives
in `docs/INSTALL.md` (right next to this file).

Open that file. It has:

- §0 — Install Ollama (required by all tracks)
- §1 — Windows (local dev, Docker, production notes)
- §2 — macOS (local dev, Docker, production notes)
- §3 — Linux / Ubuntu 22.04+ (local dev, Docker, VPS production)
- §4 — First-run checks (health, preflight, auth, footer, chat)
- §5 — Uninstall / reset (stop stack, wipe repo, wipe Ollama, nuclear)

The other entry points in this directory are `README.md` and
`docs/DIAGRAMS.md`.

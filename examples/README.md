# Examples

Use the CLI demo:

```powershell
python -m constitutional_builder.cli
```

When running from the repository without installation, set `PYTHONPATH=source`
or use the tests and benchmark scripts, which add `source/` automatically.

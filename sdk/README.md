# SDK

SDKs bind to the same server-side kernel authorization semantics. SDKs may not
synthesize successful decisions or bypass policy/capability checks.

- `python/` - Python client.
- `typescript/` - Fetch-based TypeScript client.
- `powershell/` - Windows/operator PowerShell client.
